<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="starrail.sharetext.mappers.reservationMapper">

<!-- 1단계 예약등록 -->
<insert id="rCreate"  parameterType="ReservationVO">
	insert into reservation (res_no, m_id, res_sdate, res_tcount, i_name )
		values(reservation_seq.nextval, #{m_id}, #{res_sdate}, #{res_tcount}, #{i_name})
</insert>


<!-- 발권역 정보 가져오기 -->
<select id="issulistAll" resultType="IssuInfoVO">
	<![CDATA[
	select 
		i_name 
	from 
		issuinfo
		where i_name >0
	]]>
</select> 

<!-- 예약 정보 가져오기 -->
<select id="reservationRead" resultType="ReservationVO">
	<![CDATA[
		select 
			m_id,res_sdate,res_tcount, i_name
			from
			reservation
			where res_no=#{res_no}
	]]>
</select>



</mapper>